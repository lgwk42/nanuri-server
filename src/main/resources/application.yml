server:
  port: 8080

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/[schema]?serverTimezone=Asia/Seoul&characterEncoding=UTF-8
    username: [db username]
    password: [db password]
    driver-class-name: com.mysql.cj.jdbc.Driver

  security:
    user:
      name: nanuri
      password: nanuri1234!

  jpa:
    database: mysql
    database-platform: org.hibernate.dialect.MySQL8Dialect
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
        format_sql: true
    hibernate:
      ddl-auto: update
    show-sql: true

application:
  jwt:
    secretKey: a51044330f01744d8d9c88632228a39ab4bb306274ae30277aff215918445a2c
    expiration: 172800000    # 48시간
    refreshExpiration: 604800000   # 7일
